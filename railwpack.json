{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["npm ci --production"]
    }
  },
  "deploy": {
    "startCommand": "node main.js"
  }
}
